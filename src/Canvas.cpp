//canvas.cpp
#include "Canvas.h"

// Canvas is intentionally implemented inline in Canvas.h.  Keeping this
// translation unit avoids changing the existing target layout while removing
// the old duplicate class definition that could silently diverge from it.
